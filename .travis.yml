dist: trusty
language: php

php:
  - 7.1

sudo: required

services:
  - docker

before_script: 
  - composer install
  - cd docker
  - ./start.sh
  - ./set_correct_permissions.sh
  - ./import_db.sh

script:
  - vendor/bin/phpunit
  - cd docker
  - ./tests.sh
